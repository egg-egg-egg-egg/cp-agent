# CP-Agent 出题最佳实践

> 基于 2026-09-23 实测：3 道题完整跑通（2 成功 / 1 失败后补跑），含耗时、token、
> 导出包结构校验与上传链路。所有命令在 `D:\Workspace\workbuddy\cp-agent` 下执行。

---

## 0. 一句话流程

```
构思题目 → LLM 自驾调用 14 个 tool → 编译/造数据/对拍/强度检查 → 导出平台包 → 上传 OJ
```

全程一条命令，人工只在最后上传时需要介入。

---

## 1. 环境准备（三项，缺一不可）

```
环境三件套
├── Python          必须用项目自带 venv
│   D:\Workspace\workbuddy\cp-agent\.venv\Scripts\python.exe
│   （托管 python 零依赖，直接跑 main.py 会 ModuleNotFoundError）
├── C++ 编译器      g++（本机来自 RedPanda-Cpp\MinGW64，已在 PATH）
│   编译 solution / generator / validator / naive 全靠它
└── 凭证            环境变量（config.yaml 里只存 env_key，不存明文）
    DEEPSEEK_API_KEY / OJ_USER / OJ_PASSWORD
```

验证环境：

```bash
.venv\Scripts\python.exe main.py --list-providers    # 配置链路是否通
.venv\Scripts\python.exe main.py --list-topics       # 可用算法主题
.venv\Scripts\python.exe main.py --list-difficulties # 23 档难度
```

### 凭证的两种方式

| 方式 | 适用 | 说明 |
|---|---|---|
| 系统环境变量 | **推荐** | 写入 `HKCU\Environment` 后**必须新开终端**才生效 |
| 从注册表注入子进程 | 无人值守跑批 | 避免 Git Credential Manager 弹 GUI 卡死（见 §8） |

`config.yaml` 里 deepseek 段现在只有 `env_key: "DEEPSEEK_API_KEY"`，明文已移除。

---

## 2. 出一道题：核心命令

```bash
.venv\Scripts\python.exe main.py \
    --topic dp --difficulty 1500 --name my_problem \
    --test-count 30 --export-after hydrooj
```

常用参数速查：

```
--topic          算法主题（dp/tree/graph/greedy/string/math/...，共 20 个）
--difficulty     CF 分数 800-3000，步长 100（共 23 档）
--name           题目目录名，同时是 file_io 文件名前缀
--test-count     测试点数量（默认 30，对齐 Hydro 示例）
--stress         对拍轮数（默认 10000）
--max-iterations LLM 自驾上限（默认 30，见 §8 失败处理）
--export-after   生成成功后自动导出：hydrooj / luogu / polygon / all
--idea / --idea-file   题意完善模式：给大致题意让系统补全（--topic 变可选）
--cross-check    独立验题：另一模型盲解并与标程全测试点比对
--difficulty-review    难度校准：独立评审估 CF rating，偏差 >300 告警
```

产物目录：

```
problems/<name>/
├── problem.md      题面（Markdown + LaTeX）
├── problem.yaml    元数据（标题/标签/难度/时限/测试点/校验结果）
├── result.json     本次生成的结构化结果（成功与否/耗时/token/各步状态）
├── solution.cpp    标程
├── generator.cpp   数据生成器（argv[3]=="stress" 时生成小数据）
├── validator.cpp   输入校验器
├── naive.cpp       暴力解（用于对拍和强度检查）
├── bin/ inputs/ outputs/
└── export/hydrooj/<时间戳>.zip
```

失败的题目**不会留在** `problems/`：空目录直接删，有产物的归档到 `problems/failed/<name>_<时间戳>/`。

---

## 3. 难度体系：CF 分数 ↔ CSP-J2 复赛对标

`--difficulty` 是 Codeforces rating，**只表示难度，不绑定算法或数据规模**
（算法由 `--topic` 指定，N/时限/内存由 LLM 自行决定）。

对标 2025 CSP-J2 第二轮（入门级，四题传统型、各 1.0 秒：
拼数 / 座位 / 异或和 / 多边形）：

| 复赛题位 | CF rating | 本次实测选题 |
|---|---|---|
| T1 入门 | 800–1000 | `string 900` |
| T2 | 1000–1300 | `greedy 1200` |
| T3 | 1300–1600 | `dp 1500` |
| T4 | 1600–1900 | — |

> 知识库里 2023/2024/2025 三份复赛 PDF 均为 `can_fetch_content: false`，
> 正文取不到；2025 的题目清单来自文件摘要。

---

## 4. 流水线：7 步 + 三道质量门禁

```
1. 编译        generator / validator / solution / naive（多解题另有 checker）
2. 生成数据    运行 generator 产出随机 + 边界数据
3. 校验输入    validator 检查合法性，并统计每个约束的 min/max 是否被触达
4. 生成输出    solution 跑出 .out
5. 对拍        naive vs solution，数千组（有 checker 则走 SPJ）
6. 强度检查    solution 必须在时限内跑过最大数据；naive 必须被卡掉
7. 终检        文件齐全 + 样例一致 + 边界覆盖 + 数据强度，全过才算完成
```

门禁（Agent 模式代码级强制）：

```
门禁
├── 查重         search_problem_db 相似度触发 → LLM 裁判判「同一题目模型」
│                判撞题则拦截造数据/对拍/写元数据/终检，逼 LLM 换题
├── 完成前终检   不通过不允许结束
└── pre-commit   commit 时扫暂存区，命中凭证特征直接拒绝提交
```

**当前状态提醒**：查重库（`problem_data/`）未下载、`db` 依赖未装，
查重处于**静默降级**——不会报错，题照样出，但防撞题那层网是失效的。
要恢复需：`pip install -e ".[db]"` + 下载题库到 `problem_data/`。

---

## 5. 导出：HydroOJ 包结构（与示例逐字段一致）

```bash
# 独立导出
.venv\Scripts\python.exe export.py problems/<name> --format hydrooj
# 或生成时自动导出
... main.py ... --export-after hydrooj
```

格式名：`hydrooj`（`hydro` 是兼容别名，也可用）。

包内结构（已对照 `static/Hydro格式示例.zip` 校验，字段无缺失无多余）：

```
<YYYYMMDD_HHMMSS>/
├── problem.json       title / time / memory / description / input / output /
│                      samples[{input,output,title,explain}] / hint /
│                      dataRange / tags[] / difficulty / file_io{input,output}
├── problem.yaml       title / tag[] / difficulty
├── problem_zh.md      中文题面（Markdown + LaTeX）
└── testdata/
    ├── config.yaml    time / memory / filename / subtasks[{score,type,cases[]}]
    ├── input.name     例如 cspj_t1_string.in
    ├── output.name    例如 cspj_t1_string.out
    └── <filename>1..30.in / .out
```

关键约定：

- `file_io` 开启时（config `hydro.file_io: true`），题面里会写
  「本题的输入文件为 X.in，输出文件为 X.out」，并生成 `input.name`/`output.name`。
- `filename` 留空则用 **slug（即 `--name`）** 作前缀。
- 子任务默认 `type: min`（子任务内取最低分），30 个测试点归一个 subtask。
- 其他格式：`luogu`（扁平 data.zip）、`polygon`、`xml`（HUSTOJ FPS）。

---

## 6. 上传到 OJ

```bash
# 先预演：只解析校验，不登录不上传
.venv\Scripts\python.exe upload.py problems/<name> --kind hydro2 --dry-run

# 正式上传（上传后默认「未启用」，要启用加 --enable）
.venv\Scripts\python.exe upload.py problems/<name> --kind hydro2
.venv\Scripts\python.exe upload.py problems/<name> --kind hydro2 --enable

# 验收：只读核对题面/用例/时限/启用状态是否真的落地
.venv\Scripts\python.exe oj_check.py ...
```

要点：

```
上传
├── --kind      hydro2（新一代入口，题面按 Markdown 渲染，推荐）
│               hydro / xml / qduoj（老一代，把 HTML 当 markdown，格式会散）
├── 凭证来源    命令行 > upload_config.toml > 环境变量
│               OJ_BASE_URL / OJ_USER / OJ_PASSWORD / OJ_KIND
├── --enable    不加则新题「未启用」（本站新建题的常态）
└── --dry-run   强烈建议先跑一次
```

> 凭证已迁到环境变量 `OJ_USER` / `OJ_PASSWORD`（见 §1），
> `upload_config.toml` 里不再有明文密码。

---

## 7. 实测数据（本次 3 题 + 历史 1 题）

| 题目 | topic | 难度 | 结果 | 迭代 | 耗时 | input token | output token |
|---|---|---|---|---|---|---|---|
| cspj_t1_string | string | 900 | ✅ | 17 | 84.9 min | 227,048 | 14,819 |
| cspj_t2_greedy | greedy | 1200 | 首次 ❌ → 补跑 ✅ | 30 → 13 | 6.4 → 85.5 min | 148,041 | 9,491 |
| cspj_t3_dp | dp | 1500 | ✅ | 15 | 85.4 min | 248,163 | 14,518 |
| smoke_dp2（历史） | dp | 1000 | ✅ | 13 | 27.1 min | 265,452 | 18,353 |

**成本预期**：单题 **27–86 分钟**、**15–27 万 input token**。
批量出题前先算预算；token 大头在 LLM 自驾的多轮上下文累积。

三道题的最终终检均全绿：

```
cspj_t1_string   样例 3/3   测试点 30 组   数据强度 OK
cspj_t2_greedy   样例 2/2   测试点 30 组   数据强度 OK   中文比例 90.98%
cspj_t3_dp       样例 2/2   测试点 30 组   数据强度 OK
```

> 导出包体积差异很大（731 KB / 1.9 MB / 19 MB），取决于数据规模上限
> —— t2 的 $n \le 2\times10^5$ 让测试数据显著膨胀，上传前注意包大小。

---

## 8. 无人值守跑批（睡前挂机的关键）

三件事必须处理，否则半夜会卡死：

```
1. 凭证注入 —— 不要依赖已运行终端的环境变量
   从注册表 HKCU\Environment 读取后注入子进程 env：
       env[name] = winreg.QueryValueEx(...)
   否则 Git Credential Manager 弹 GUI 无人点，命令挂到超时被 SIGTERM。

2. 后台运行 + 日志落盘
   把 stdout 同时写到日志文件，避免只靠终端缓冲。

3. 网络波动
   GitHub/OJ 可能间歇 502；push 用 --force-with-lease 而非裸 -f。
```

失败处理：

- **撞 `--max-iterations`**（本次 t2 首次：6.4 分钟就耗尽 30 次迭代，属「卡住」而非「慢」）：
  把上限放宽到 45 重试一次即可 —— 补跑时**只用了 13 次迭代**就通过了，
  说明首次是 LLM 陷进了某个重试循环，不是题目本身做不出来。
  若放宽后仍失败，建议换 `--topic`。
- **归档目录为空**（无产物被清理）：原因要查 `cp_agent.log`；
  该日志会被后续运行轮转覆盖，失败后要及时取证。

---

## 9. 踩坑清单（按被坑概率排序）

```
1. 用错 python          → 必须用 .venv\Scripts\python.exe
2. 改完环境变量没开新终端 → 注册表写入不传播到已运行进程
3. 迁移凭证顺序反了      → 必须先写环境变量，再删明文（否则 ConfigError 卡死）
4. 以为查重在生效        → 题库缺失时静默降级，不报错但没防护
5. 用 --format hydro    → 可用（别名），但 --export all 不含它，显式写 hydrooj 更稳
6. 修改历史在主工作区做  → 会误伤未忽略的目录（本仓库已把 .workbuddy/ 排除）
7. 本地清了 .git 以为远端也清了 → force push 不删服务器旧对象（见下）
```

### 一条重要的安全认知

凭证一旦进过**公开仓库**，改历史只能保证「未来 clone 的人看不到」，
撤不回「已经发出去的内容」——GitHub 对 unreachable 对象有保留期，
旧 commit 通过 sha 仍可访问。唯一真正的补救是**轮换**。

---

## 10. 推荐工作流

```bash
# ① 先小成本冒烟（低难度 + 少测试点）
.venv\Scripts\python.exe main.py --topic string --difficulty 900 \
    --name smoke --test-count 10

# ② 正式出题（30 测试点 + 自动导出 Hydro 包）
.venv\Scripts\python.exe main.py --topic dp --difficulty 1500 \
    --name real_problem --test-count 30 --export-after hydrooj

# ③ 看 result.json 确认各项都过
cat problems/real_problem/result.json

# ④ 上传前预演
.venv\Scripts\python.exe upload.py problems/real_problem --kind hydro2 --dry-run

# ⑤ 上传并验收
.venv\Scripts\python.exe upload.py problems/real_problem --kind hydro2 --enable
.venv\Scripts\python.exe oj_check.py ...
```

批量出题用 `batch_generate.py`（断点续跑，每题以 `result.json` 为准）：

```bash
.venv\Scripts\python.exe batch_generate.py --limit 2   # 先冒烟
.venv\Scripts\python.exe batch_generate.py              # 全量
```
