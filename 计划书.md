# 计划书：题意完善模式（Idea Mode）

> **状态：已实现并验证（2026-07-26）**。实现中新增一项计划外改进：查重召回升级为
> 多路（LLM 关键词 + 题面标题 + 描述首段），原因见文末"实现备注"。

## 背景与目标

当前 CP-Agent 只有两种入口，覆盖不了出题人最常见的工作流：

| 模式 | 入口 | 局限 |
|------|------|------|
| Agent 自由构思 | `--topic` + `--difficulty` | 题目由 LLM 从零构思，用户只能靠 `--extra` 间接影响，题意容易跑偏 |
| Pipeline 验收 | `--pipeline <目录>` | 要求全部文件已写好，只做编译/造数据/对拍，不做补全 |

**目标**：新增"题意完善模式"——用户给一段大致题意（一句话想法、半成品题面、或已有草稿文件），系统在**不改变核心题目模型**的前提下，把它完善成规范题面 + 标程 + 生成器 + 校验器 + 暴力解 + 测试数据的完整题目包，并走完全部质量门槛。

## 功能设计

### 1. CLI 入口（main.py）

```bash
# 直接给题意
python main.py --idea "给一棵树，每次删一条边问连通块内第k大，考主席树+启发式合并" --difficulty 2100

# 题意较长/已有草稿时用文件
python main.py --idea-file draft.md --difficulty 1800

# 题意与题库撞题时默认中止；确认无妨后用 --allow-dup 继续
python main.py --idea "..." --allow-dup
```

- `--idea <文本>` / `--idea-file <路径>`：二选一，同时给报错；给了任一个后 `--topic` 变为**可选**（由 LLM 从题意推断考点）
- `--allow-dup`：撞题时继续生成（默认中止）
- 其余参数（`--difficulty`、`--name`、`--test-count`、`--stress`、`--export-after` 等）与现有模式通用

### 2. Prompt 分支（agent.py: build_user_prompt / SYSTEM_PROMPT）

`build_user_prompt` 增加 `idea` 参数，进入完善模式时任务描述变为：

- 「以下是出题人给定的题意，你的任务是**补全和规范化**，不得改变核心题目模型（输入结构、核心约束、目标函数、预期解法）」
- 允许做的事：补充/收紧数据范围、确定时限内存、设计样例、规范题面表述、补全题解说明
- 不允许做的事：换题、改目标函数、改核心操作；查重撞题时**不要自行换题**（由系统按策略处理）

SYSTEM_PROMPT 增加一小节说明完善模式的行为差异。

### 3. 查重策略分化（agent.py: agent_loop）

新增 `dedup_policy` 参数，三种取值：

| 策略 | 适用 | must_change 时的行为 |
|------|------|---------------------|
| `rewrite`（现状） | 自由构思模式 | 拦截产出工具 + 注入"换题重写"提示，逼 LLM 换题 |
| `abort`（新，默认） | 完善模式 | **立即终止** agent loop，返回失败；failure_reason 带裁判找到的原题与理由，写入 result.json |
| `warn`（新） | 完善模式 + `--allow-dup` | 判定降级为 manual_review：不拦截、不终止，仅在结果中保留裁判理由供参考 |

理由：题意是用户给定的，"强制换题"违背用户意图；非交互环境下 fail-fast + 明确原因最诚实，用户看过撞题详情后可自行决定 `--allow-dup` 重跑。

### 4. 半成品目录支持（agent.py: generate_problem）

`--name` 指向的目录已存在且含 problem.md（或部分 cpp）时：

- 扫描已有文件清单，注入用户 prompt：「目录中已有以下文件：…，请先 read_file 查看，在其基础上**增量补全**；用户已写内容视为题意的一部分，同样不得推翻」
- 不清空目录、不覆盖用户已有文件语义（LLM 仍可通过 edit_file 修格式错误）

### 5. GUI（gui.py 生成页）

- 新增多行「题意描述」输入框（QPlainTextEdit，约 4 行高，placeholder 提示用法）
- 新增「撞题仍继续 (--allow-dup)」勾选框
- 题意非空时自动附加 `--idea`（topic 下拉仍生效，作为辅助提示传入）

## 不改变的部分

- 查重的检索召回 + LLM 裁判机制本身不动（只分化 must_change 后的处置策略）
- 全部质量门槛照常生效：中文校验、边界覆盖、数据强度、final_check、完成门禁
- result.json / 失败归档 / 导出流程不动

## 改动文件清单

| 文件 | 改动 |
|------|------|
| `main.py` | `--idea` / `--idea-file` / `--allow-dup`；参数校验（topic 与 idea 至少其一） |
| `agent.py` | `build_user_prompt(idea=…)` 分支；`agent_loop(dedup_policy=…)` 三策略；`generate_problem(idea=…, allow_dup=…)` 组装策略 + 半成品目录检测；SYSTEM_PROMPT 完善模式说明 |
| `gui.py` | 生成页题意输入框 + allow-dup 勾选 |
| `tests/test_idea_mode.py`（新增） | prompt 分支断言；abort 策略立即终止；warn 策略不拦截；半成品文件清单注入 |
| `README.md` / `AGENT.md` | 用法与流程说明 |

## 验证方式

1. **单测（不调 LLM）**：脚本化假 LLM 驱动 agent_loop——abort 策略下查重 must_change 返回失败且带 duplicates；warn 策略下产出工具不被拦截；prompt 含"不得改变核心题目模型"与已有文件清单
2. **真实端到端（阳性）**：`--idea` 给一个复刻 P4309 的题意 → 预期启动后在查重步中止，result.json 的 failure_reason 指明撞了 P4309；加 `--allow-dup` 重跑 → 继续生成
3. **真实端到端（正常）**：给一个原创题意跑通全流程，确认最终题面保持了给定的题目模型
4. GUI 离屏截图确认新控件布局

## 工作量估计

代码 + 测试约半天；真实端到端验证视 LLM 生成时长而定（单题 5-20 分钟）。

## 实现备注（2026-07-26）

- 全部按计划落地；95 个单测通过
- **计划外改进（多路召回）**：首次真实验证时发现撞题用例没触发 abort——不是裁判的问题，
  而是召回层没把原题送到裁判面前：LLM 自己写的 query 向量分只有 0.0-0.46（低于 0.5 触发线）。
  诊断发现整段题面 embedding 会稀释语义（P4309 召不回），而标题作 query 时 P4309 排第 1（0.79）。
  于是 `_dedup_check` 召回改为三路合并：LLM 关键词 + 题面标题 + 描述首段
- 顺手修复：`problem_db.embedder.get_embedder` 无缓存，每次搜索重载模型（现按 model_key 缓存）
- 追加保护：半成品目录（含用户草稿）生成失败时原地保留 + 写 result.json，不再归档挪走
- 真实端到端结果：复刻 P4309 的题意 → 多路召回 0.74 → 裁判判同模型（理由含解法）→ abort
  并提示 --allow-dup；加 --allow-dup 重跑 → 降级为警告继续生成 ✅
